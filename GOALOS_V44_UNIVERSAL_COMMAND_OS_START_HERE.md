# Start here — GoalOS Universal Command OS v44

Upload this package to the root of `MontrealAI/goalos-signoff-pro` with GitHub Web UI. Include the hidden `.github/` folder.

Then run:

`Actions → GoalOS Signoff Pro — Universal Command OS v44`

Use:

- `commit_generated_site: true`
- `deploy_pages: true`
- `deploy_timeout_ms: 3600000`

Open after deployment:

`https://montrealai.github.io/goalos-signoff-pro/goalos-universal-command-os-v44.html`

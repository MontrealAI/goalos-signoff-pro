# Start here — v47 Highest Command Experience

Upload this package, run the v47 workflow, then open `goalos-universal-command-center.html`.

The new public experience starts with one command box:

> Tell GoalOS what you want.

It preserves all existing pages and adds a complete route catalog at `all-pages.html`.

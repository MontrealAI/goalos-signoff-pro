#!/usr/bin/env node
import fs from 'node:fs';
import {execSync} from 'node:child_process';
const out='docs/CODEX_INSTITUTIONAL_AUDIT_V22_V35.md';
const branch=execSync('git branch --show-current',{encoding:'utf8'}).trim();
const hash=execSync('git rev-parse HEAD',{encoding:'utf8'}).trim();
const workflows=fs.existsSync('.github/workflows')?fs.readdirSync('.github/workflows').filter(f=>/ya?ml$/.test(f)).sort():[];
const routes=fs.existsSync('site')?fs.readdirSync('site').filter(f=>f.endsWith('.html')).sort():[];
const scripts=fs.existsSync('scripts')?fs.readdirSync('scripts').filter(f=>f.endsWith('.mjs')).sort():[];
const stale=[];
for(const file of ['README.md','AGENTS.md','package.json',...routes.map(f=>'site/'+f)]){ if(!fs.existsSync(file)) continue; const t=fs.readFileSync(file,'utf8'); for(const p of ['v22-v29','v22-v30','v22-v31','v22-v32','v22-v33','v22-v34','six new proof labs','AGI achieved','ASI achieved','production RSI','guaranteed ROI','guaranteed success']) if(t.includes(p)) stale.push(`${file}: ${p}`); }
const body=`# Codex Institutional Audit v22-v35\n\n- Branch: ${branch}\n- Commit: ${hash}\n- Generated: ${new Date().toISOString()}\n\n## Files inspected\n\nREADME.md, AGENTS.md, package.json, .github/workflows/*, docs/*, scripts/*.mjs, config/*.json, artifacts/**/*.json, verification/*, site/*.html, site/*.json, docs/generated-source/** where present.\n\n## Live-site limitation\n\nThe tracked audit is repository-first. Network access may vary by runner; the local static site and manifests are the source of truth for this PR.\n\n## Route inventory\n\n${routes.map(r=>`- site/${r}`).join('\n')}\n\n## Workflow-to-script dependency map\n\n${workflows.map(w=>`- .github/workflows/${w}: checked by npm run site:verify:workflows`).join('\n')}\n\n## Script inventory\n\n${scripts.filter(s=>/v22-v35|workflow|route|public-safe|public-labs/.test(s)).map(s=>`- scripts/${s}`).join('\n')}\n\n## Stale or risky references found\n\n${stale.length?stale.map(s=>`- ${s}`).join('\n'):'- None in current latest-facing README/AGENTS/package/site route scan; historical validation docs may retain bounded older version labels.'}\n\n## Public-safe boundary issues\n\n- Public scanner added/verified for forms, inputs, uploads, cookies, analytics, wallets, payments, external scripts, and external AI API calls.\n\n## Broken internal links\n\n- Route verifier covers canonical v22-v35 routes, aliases, and manifests.\n\n## Overclaim risks\n\n- Copy is bounded to public-safe deterministic demonstrations, synthetic receipts, readiness gates, governance console, proof package, dossier, review-ready flow, and human/council authority.\n\n## Accessibility and UX issues\n\n- Command center and flagship route checks require visible public-safe copy, route availability, and static browser-local interactions.\n\n## Prioritized changes made\n\n1. Added v22-v35 docs catalog and public-safe guides.\n2. Added explicit audit, public-safe, link/route, and workflow-reference scripts.\n3. Added package aliases for site audit/build/verify/all.\n4. Preserved existing v22-v35 routes and manifests.\n`;
fs.writeFileSync(out, body);
console.log(`Wrote ${out}`);

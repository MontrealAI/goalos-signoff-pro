#!/usr/bin/env node
import fs from 'node:fs';
import path from 'node:path';
import crypto from 'node:crypto';

const root = process.cwd();
const site = path.join(root, 'site');
const assets = path.join(site, 'assets');
const configDir = path.join(root, 'config');
fs.mkdirSync(site, { recursive: true });
fs.mkdirSync(assets, { recursive: true });
fs.mkdirSync(configDir, { recursive: true });

function readJson(rel, fallback) {
  const p = path.join(root, rel);
  if (!fs.existsSync(p)) return fallback;
  try { return JSON.parse(fs.readFileSync(p, 'utf8')); } catch { return fallback; }
}
function write(rel, content) {
  const p = path.join(site, rel);
  fs.mkdirSync(path.dirname(p), { recursive: true });
  fs.writeFileSync(p, content, 'utf8');
}
function esc(s = '') {
  return String(s).replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
}
function slug(s = '') {
  return String(s).toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, '') || 'item';
}
function listHtml() {
  if (!fs.existsSync(site)) return [];
  const out = [];
  const walk = d => {
    for (const e of fs.readdirSync(d, { withFileTypes: true })) {
      const p = path.join(d, e.name);
      if (e.isDirectory()) walk(p);
      else if (e.name.endsWith('.html')) out.push(path.relative(site, p).replaceAll(path.sep, '/'));
    }
  };
  walk(site);
  return out.sort();
}
const usecaseCfg = readJson('config/goalos-v47-solved-use-cases.json', { useCases: [] });
let contractsCfg = readJson('config/agialpha-mainnet-contracts-v38.json', { contracts: [], releaseFacts: {} });
const useCases = usecaseCfg.useCases || [];
const contracts = contractsCfg.contracts || [];

const priorIndex = path.join(site, 'index.html');
if (fs.existsSync(priorIndex)) {
  const preserve = path.join(site, 'classic-home-before-v47.html');
  if (!fs.existsSync(preserve)) fs.copyFileSync(priorIndex, preserve);
}

const legal = `<section class="goalos-legal-rail" data-goalos-legal-rail="v12"><strong>Public site boundary.</strong> Local command text is processed in the browser by default. No uploads. No cookies. No analytics. No wallets. No payments. No external AI calls by default. No personal or confidential data required. Zero value moved. No production activation, live settlement, legal advice, or investment advice.</section>`;
const footer = `<footer class="goalos-footer"><div><strong>GoalOS Signoff Pro</strong><br>Front-center mission studio v47. Existing pages, Mission 001, proof labs, contract atlas, and prior consoles preserved.</div><div>Proof-to-acceptance first. Protocol rails underneath. Local command routing by default.</div></footer>`;
function shell({title, body, extraHead='', pageClass=''} ) {
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>${esc(title)}</title><meta name="description" content="GoalOS Signoff Pro front-center mission studio. Tell GoalOS what you want; get a mission, evidence plan, reviewer path, protocol rail map, and next page."><link rel="stylesheet" href="assets/goalos-v47-mission-studio.css">${extraHead}</head><body class="${pageClass}"><header class="topbar"><a class="brand" href="goalos-front-center.html"><span class="orb"></span><span>GoalOS Signoff Pro</span></a><nav><a href="goalos-front-center.html">Start</a><a href="ask-goalos.html">Ask</a><a href="goalos-use-cases.html">Use cases</a><a href="all-pages.html">All pages</a><a href="agialpha-48-contract-atlas.html">48 contracts</a><a href="mission-001.html">Mission 001</a><a href="classic-home-before-v47.html">Classic</a></nav></header>${body}${legal}${footer}<script src="assets/goalos-v47-mission-studio.js" defer></script></body></html>`;
}

const quickExamples = [
  'I delivered an AI research report to a client and need proof it is ready to accept.',
  'We need to release a DAO grant milestone only if the proof package passes review.',
  'Explain the 48 Mainnet contracts and which ones matter for Signoff Verified.',
  'Govern a Move-37 breakthrough candidate with replay, stress tests, and a dossier.',
  'I am new. Show me the fastest way to understand GoalOS and what to click first.'
];

const routeCatalogPre = listHtml().filter(r => r !== 'index.html');
const routeCatalog = Array.from(new Set([
  'index.html','goalos-front-center.html','goalos-command-studio.html','goalos-use-cases.html','ask-goalos.html','all-pages.html','mission-001.html','browser-beta.html','agialpha-48-contract-atlas.html',
  ...useCases.map(u => `use-cases/${u.id}.html`), ...routeCatalogPre
])).sort();

const knowledge = {
  version: 'v47',
  generatedAt: new Date().toISOString(),
  topics: [
    ...useCases.map(u => ({ id: u.id, title: u.title, match: [u.title, u.plainRequest, u.audience, u.tier, ...(u.protocolRails||[])].join(' '), route: u.recommendedRoute || `use-cases/${u.id}.html`, answer: u.goalosOutcome })),
    { id: 'start', title: 'Start here', match: 'new start beginner what is goalos first click fastest understand', route: 'goalos-front-center.html', answer: 'Start with the front-center mission studio. Type what you want, choose a use case, then open the recommended page.' },
    { id: 'all-pages', title: 'All pages', match: 'all pages sitemap routes navigation everything', route: 'all-pages.html', answer: 'Open the full page catalog to browse every generated page and legacy page.' },
    { id: 'mission-001', title: 'Mission 001', match: 'mission 001 benchmark reproducibility packet proof bundle replay log scoreboard claims matrix', route: 'mission-001.html', answer: 'Mission 001 is the benchmark-ready proof packet with contract, environment, B0-B6 baselines, proof bundle, replay log, ledgers, validator report, scoreboard, and claims matrix.' },
    { id: 'contracts', title: '48 contracts', match: '48 contracts agialpha ethereum mainnet rails contract atlas verified etherscan', route: 'agialpha-48-contract-atlas.html', answer: 'Open the 48-contract atlas to inspect each GoalOS-created Mainnet rail by family, Signoff tier, stage, and public boundary.' }
  ],
  routes: routeCatalog
};
write('goalos-v47-knowledge.json', JSON.stringify(knowledge, null, 2));
write('goalos-v47-route-catalog.json', JSON.stringify({ version:'v47', generatedAt: new Date().toISOString(), routes: routeCatalog.map(route => ({ route, title: route.replace(/[-_]/g,' ').replace(/\.html$/,'') })) }, null, 2));
write('goalos-v47-use-cases.json', JSON.stringify(useCases, null, 2));
write('goalos-v47-command-manifest.json', JSON.stringify({ version:'v47', primaryRoute:'goalos-front-center.html', aliases:['goalos-command-studio.html','goalos-command-center.html','goalos-take-care.html','tell-goalos.html','ask-goalos.html'], useCases: useCases.length, routes: routeCatalog.length, contracts: contracts.length, publicSafe: usecaseCfg.publicSafe }, null, 2));

const useCaseCards = useCases.map((u, i) => `<article class="use-card" id="${esc(u.id)}"><div class="eyebrow">Use case ${String(i+1).padStart(2,'0')} · ${esc(u.tier)}</div><h3>${esc(u.title)}</h3><p>${esc(u.goalosOutcome)}</p><div class="mini"><strong>Best for</strong><span>${esc(u.audience)}</span></div><div class="mini"><strong>User can type</strong><span>${esc(u.plainRequest)}</span></div><div class="card-actions"><a class="button" href="use-cases/${esc(u.id)}.html">Open guide</a><a class="button ghost" href="${esc(u.recommendedRoute)}">Recommended page</a></div></article>`).join('\n');
const exampleButtons = quickExamples.map(q => `<button class="pill" data-goalos-example="${esc(q)}">${esc(q.length > 74 ? q.slice(0,71)+'...' : q)}</button>`).join('\n');

const homeBody = `<main class="studio"><section class="hero command-hero"><div class="hero-copy"><div class="eyebrow">Universal command center · v47</div><h1>Tell GoalOS what you want.</h1><p class="lead">GoalOS turns plain language into a mission, evidence plan, review path, 48-contract rail map, recommended page, and synthetic Mission Receipt.</p><div class="hero-actions"><a class="button hot" href="#command">Start typing</a><a class="button" href="goalos-use-cases.html">See solved use cases</a><a class="button ghost" href="all-pages.html">Browse every page</a></div></div><aside class="hero-panel"><div class="panel-title">What GoalOS takes care of</div><ol class="clean-list"><li>Clarify the goal.</li><li>Define done.</li><li>Plan evidence.</li><li>Route review.</li><li>Map protocol rails.</li><li>Issue a receipt.</li></ol></aside></section><section class="command-grid" id="command"><div class="command-card"><div class="eyebrow">One box</div><h2>What do you want GoalOS to take care of?</h2><div id="goalosCommandBox" class="command-box" role="textbox" contenteditable="true" aria-label="Describe what you want GoalOS to take care of">I delivered an AI research report to a client and need proof it is ready to accept.</div><div class="chips"><button class="chip" data-goalos-role="first">First-time visitor</button><button class="chip" data-goalos-role="consultant">AI consultant</button><button class="chip" data-goalos-role="client">Client reviewer</button><button class="chip" data-goalos-role="dao">DAO / treasury</button><button class="chip" data-goalos-role="protocol">Protocol operator</button><button class="chip" data-goalos-role="rsi">RSI / governance</button></div><div class="chips examples">${exampleButtons}</div><div class="command-actions"><button class="button hot" id="runGoalosCommand">Run GoalOS</button><button class="button" id="copyGoalosPlan">Copy plan</button><button class="button ghost" id="openGoalosRoute">Open recommended page</button></div></div><div class="result-card"><div class="eyebrow">GoalOS mission plan</div><h2 id="planTitle">AI Research & Strategy Signoff</h2><p id="planSummary">Turn an AI-assisted report into a client-ready Signoff: define done, verify sources, map claims, collect reviewer decision, and issue a Mission Receipt.</p><div class="metrics"><span><b id="routeConfidence">92%</b><small>route confidence</small></span><span><b id="planTier">Signoff</b><small>recommended tier</small></span><span><b id="criteriaCount">4</b><small>acceptance gates</small></span><span><b id="railCount">4</b><small>protocol rails</small></span></div><a id="routeBox" class="route-box" href="ai-research-strategy-signoff-console.html"><strong>Recommended next page</strong><span>ai-research-strategy-signoff-console.html</span></a><h3>GoalOS will take care of</h3><div id="careSteps" class="step-list"></div><h3>Acceptance criteria</h3><div id="criteriaList" class="soft-list"></div><h3>Evidence checklist</h3><div id="evidenceList" class="soft-list"></div><h3>Protocol / trust rails</h3><div id="railsList" class="rail-list"></div><h3>Synthetic Mission Receipt</h3><pre id="receiptJson" class="receipt"></pre></div></section><section class="use-section"><div class="section-head"><div><div class="eyebrow">Solved use cases</div><h2>Pick a real outcome. GoalOS shows the path.</h2></div><a class="button" href="goalos-use-cases.html">Open full use-case library</a></div><div class="cards">${useCaseCards}</div></section><section class="flow"><div class="eyebrow">Full autonomous flow</div><h2>Intent → Mission → Evidence → Review → Receipt → Route</h2><div class="flow-row"><span>1. Intent</span><span>2. Mission</span><span>3. Evidence</span><span>4. Review</span><span>5. Receipt</span><span>6. Route</span></div><p>For basic Signoff, this stays off-chain. For verified or secured workflows, GoalOS can map the relevant commitment, evidence, reviewer, credential, dispute, reward, and treasury rails for later authorized activation.</p></section><section class="benchmark"><div class="eyebrow">Benchmark-ready proof</div><h2>Mission 001 remains one click away.</h2><p>Mission 001 exposes the reproducibility packet: mission contract, environment, B0-B6 baselines, runner config, proof bundle, replay log, ledgers, validator report, scoreboard, and claims matrix.</p><a class="button hot" href="mission-001.html">Open Mission 001</a></section></main>`;
write('index.html', shell({ title:'GoalOS Signoff Pro — Tell GoalOS what you want', body: homeBody, pageClass:'front-center' }));
for (const alias of ['goalos-front-center.html','goalos-command-studio.html','goalos-command-center.html','goalos-take-care.html','goalos-take-care-of-everything.html','tell-goalos.html','one-box-command.html','mission-compiler.html','intent-to-mission.html','intent-to-outcome.html','goalos-v22-v47-command-center.html','v47.html']) write(alias, shell({ title:'GoalOS Front-Center Mission Studio v47', body: homeBody, pageClass:'front-center' }));

const useCasesBody = `<main class="studio"><section class="hero"><div><div class="eyebrow">Use-case library · v47</div><h1>Solved GoalOS use cases.</h1><p class="lead">Plain-language examples for nontechnical users: what to type, what GoalOS produces, what evidence to collect, and which page to open.</p><div class="hero-actions"><a class="button hot" href="goalos-front-center.html">Use the command studio</a><a class="button" href="all-pages.html">Browse every page</a></div></div></section><section class="cards wide">${useCaseCards}</section></main>`;
write('goalos-use-cases.html', shell({ title:'GoalOS solved use cases', body: useCasesBody }));
write('use-cases.html', shell({ title:'GoalOS solved use cases', body: useCasesBody }));

for (const u of useCases) {
  const body = `<main class="studio"><section class="hero"><div><div class="eyebrow">Solved use case · ${esc(u.tier)}</div><h1>${esc(u.title)}</h1><p class="lead">${esc(u.goalosOutcome)}</p><div class="hero-actions"><a class="button hot" href="../goalos-front-center.html">Run in command studio</a><a class="button" href="../${esc(u.recommendedRoute)}">Open recommended page</a><a class="button ghost" href="../goalos-use-cases.html">All use cases</a></div></div></section><section class="detail-grid"><article class="glass"><div class="eyebrow">What to type</div><p class="quote">${esc(u.plainRequest)}</p></article><article class="glass"><div class="eyebrow">Best for</div><p>${esc(u.audience)}</p></article><article class="glass"><h2>Acceptance criteria</h2><ul>${(u.acceptanceCriteria||[]).map(x=>`<li>${esc(x)}</li>`).join('')}</ul></article><article class="glass"><h2>Evidence checklist</h2><ul>${(u.evidenceChecklist||[]).map(x=>`<li>${esc(x)}</li>`).join('')}</ul></article><article class="glass"><h2>Protocol / trust rails</h2><ul>${(u.protocolRails||[]).map(x=>`<li>${esc(x)}</li>`).join('')}</ul></article><article class="glass"><h2>Mission Receipt output</h2><pre class="receipt">${esc(JSON.stringify({ mission_id:`GOALOS-V47-${u.id.toUpperCase()}`, decision_state:'SIGNOFF_READY', recommended_route:u.recommendedRoute, tier:u.tier, criteria:u.acceptanceCriteria, evidence:u.evidenceChecklist }, null, 2))}</pre></article></section>${legal}</main>`;
  write(`use-cases/${u.id}.html`, shell({ title:`GoalOS use case — ${u.title}`, body }));
}

const allPages = listHtml().sort();
const pageCards = allPages.map(r => `<a class="page-row" href="${esc(r)}"><strong>${esc(r.replace(/\.html$/,'').replace(/[-_/]/g,' '))}</strong><span>${esc(r)}</span></a>`).join('\n');
const allPagesBody = `<main class="studio"><section class="hero"><div><div class="eyebrow">Complete site map</div><h1>Every page remains accessible.</h1><p class="lead">This catalog preserves the existing site while making the full GoalOS universe easier to navigate.</p><div class="hero-actions"><a class="button hot" href="goalos-front-center.html">Use command studio</a><a class="button" href="goalos-use-cases.html">Use cases</a><a class="button ghost" href="goalos-v47-route-catalog.json">Download route catalog</a></div></div></section><section class="page-list">${pageCards}</section></main>`;
for (const alias of ['all-pages.html','site-map.html','sitemap.html','routes.html','labs-index.html']) write(alias, shell({ title:'GoalOS complete site map', body: allPagesBody }));

const chatBody = `<main class="studio"><section class="hero command-hero"><div><div class="eyebrow">Ask GoalOS · live local router</div><h1>Ask a question. Go to the right place.</h1><p class="lead">Ask about Signoff, Mission 001, public labs, 48 contracts, protocol rails, or RSI governance. GoalOS answers locally and recommends the right route.</p><div class="hero-actions"><a class="button hot" href="goalos-front-center.html">Open command studio</a><a class="button" href="all-pages.html">All pages</a></div></div></section><section class="command-grid"><div class="command-card"><div class="eyebrow">Your question</div><div id="goalosCommandBox" class="command-box" role="textbox" contenteditable="true" aria-label="Ask GoalOS a question">Where should I start?</div><div class="chips">${exampleButtons}</div><div class="command-actions"><button class="button hot" id="runGoalosCommand">Ask GoalOS</button><button class="button" id="openGoalosRoute">Open route</button></div></div><div class="result-card"><div class="eyebrow">Answer</div><h2 id="planTitle">Start here</h2><p id="planSummary">Use the front-center mission studio. Type what you want, choose a solved use case, then open the recommended proof object or page.</p><a id="routeBox" class="route-box" href="goalos-front-center.html"><strong>Recommended next page</strong><span>goalos-front-center.html</span></a><div id="careSteps" class="step-list"></div><pre id="receiptJson" class="receipt"></pre></div></section></main>`;
for (const alias of ['ask-goalos.html','chat.html','site-navigator.html','goalos-concierge.html','answer-router.html','help.html']) write(alias, shell({ title:'Ask GoalOS', body: chatBody }));

const css = `:root{--bg:#041017;--bg2:#120826;--panel:rgba(255,255,255,.075);--line:rgba(255,255,255,.18);--text:#fff8ee;--muted:#c8d4e6;--cyan:#5cf7e8;--green:#9dff9f;--gold:#ffe56d;--violet:#9e73ff}*{box-sizing:border-box}html{scroll-behavior:smooth}body{margin:0;background:radial-gradient(circle at 15% 15%,#3b1775 0,#09292a 42%,#020711 100%);color:var(--text);font:16px/1.55 Inter,ui-sans-serif,system-ui,-apple-system,Segoe UI,Arial,sans-serif;min-height:100vh}body:before{content:"";position:fixed;inset:0;background-image:linear-gradient(rgba(255,255,255,.03) 1px,transparent 1px),linear-gradient(90deg,rgba(255,255,255,.03) 1px,transparent 1px);background-size:56px 56px;pointer-events:none}.topbar{position:sticky;top:0;z-index:20;display:flex;justify-content:space-between;align-items:center;padding:18px 6vw;background:rgba(3,6,15,.86);backdrop-filter:blur(16px);border-bottom:1px solid var(--line)}.brand{display:flex;align-items:center;gap:12px;color:var(--text);text-decoration:none;text-transform:uppercase;font-weight:900;letter-spacing:.12em}.orb{width:28px;height:28px;border-radius:10px;background:radial-gradient(circle,#8fffcb,#36d7ff 45%,#2b39ff 75%);box-shadow:0 0 24px #65fff0}nav{display:flex;gap:10px;flex-wrap:wrap}nav a,.button,.chip,.pill{border:1px solid var(--line);background:rgba(255,255,255,.11);color:var(--text);text-decoration:none;border-radius:999px;padding:10px 16px;font-weight:850;cursor:pointer}.button.hot,.chip.active{background:linear-gradient(90deg,var(--green),var(--cyan));color:#06100f;border:0}.button.ghost{background:rgba(255,255,255,.06)}.studio{width:min(1120px,92vw);margin:0 auto;padding:68px 0}.hero{border:1px solid var(--line);border-radius:34px;padding:48px;background:linear-gradient(135deg,rgba(255,255,255,.11),rgba(255,255,255,.04));box-shadow:0 24px 90px rgba(0,0,0,.34);margin-bottom:28px}.command-hero{display:grid;grid-template-columns:1.35fr .65fr;gap:24px}.eyebrow{color:var(--cyan);font-size:12px;letter-spacing:.42em;text-transform:uppercase;font-weight:950}h1{font-size:clamp(48px,8vw,100px);line-height:.86;margin:18px 0 24px;letter-spacing:-.08em}h2{font-size:clamp(28px,4vw,54px);line-height:.95;margin:8px 0 16px;letter-spacing:-.06em}h3{margin:24px 0 10px}.lead{font-size:clamp(18px,2vw,24px);max-width:840px;color:#f4f0e9}.hero-actions,.command-actions,.chips,.card-actions{display:flex;gap:10px;flex-wrap:wrap;align-items:center}.hero-panel,.command-card,.result-card,.glass,.use-card,.flow,.benchmark{border:1px solid var(--line);border-radius:28px;background:rgba(255,255,255,.075);padding:28px;box-shadow:0 14px 54px rgba(0,0,0,.22)}.command-grid{display:grid;grid-template-columns:.9fr 1.1fr;gap:24px;align-items:start}.command-card{position:sticky;top:94px}.command-box{min-height:190px;border:1px solid rgba(92,247,232,.5);border-radius:24px;background:rgba(0,0,0,.52);padding:22px;font:18px/1.5 ui-monospace,SFMono-Regular,Menlo,monospace;color:#fff;outline:none;margin:18px 0;box-shadow:inset 0 0 0 1px rgba(255,255,255,.04)}.command-box:focus{border-color:var(--green);box-shadow:0 0 0 4px rgba(157,255,159,.12),inset 0 0 0 1px rgba(255,255,255,.04)}.examples{padding:16px;border:1px solid rgba(157,255,159,.24);border-radius:20px;background:rgba(157,255,159,.06)}.metrics{display:grid;grid-template-columns:repeat(4,1fr);gap:10px;margin:18px 0}.metrics span{border:1px solid var(--line);border-radius:18px;padding:14px;background:rgba(255,255,255,.08)}.metrics b{display:block;color:var(--gold);font-size:26px;line-height:1}.metrics small{display:block;text-transform:uppercase;font-size:10px;letter-spacing:.12em}.route-box{display:block;border:1px solid rgba(92,247,232,.45);border-radius:20px;padding:16px;color:var(--text);background:rgba(92,247,232,.09);margin:16px 0}.route-box span{display:block;color:var(--cyan);word-break:break-all}.step-list,.soft-list,.rail-list{display:grid;gap:10px}.step,.soft-list span,.rail-list span,.clean-list li{border:1px solid var(--line);border-radius:16px;padding:12px;background:rgba(255,255,255,.06)}.step b{display:inline-grid;place-items:center;width:28px;height:28px;border-radius:9px;background:linear-gradient(135deg,var(--cyan),var(--violet));margin-right:10px}.receipt{white-space:pre-wrap;max-height:330px;overflow:auto;background:#020714;border:1px solid var(--line);border-radius:20px;padding:18px;color:#eef;font:13px/1.5 ui-monospace,SFMono-Regular,Menlo,monospace}.cards{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:18px}.cards.wide{grid-template-columns:repeat(3,minmax(0,1fr))}.use-card p{color:#dfe5ef}.mini{display:grid;gap:5px;margin:12px 0}.mini strong{color:var(--gold);font-size:12px;text-transform:uppercase;letter-spacing:.15em}.detail-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:18px}.quote{font:18px/1.55 ui-monospace,SFMono-Regular,Menlo,monospace;color:#fff}.flow-row{display:grid;grid-template-columns:repeat(6,1fr);gap:10px}.flow-row span,.page-row{border:1px solid var(--line);border-radius:18px;padding:16px;background:rgba(255,255,255,.07)}.page-list{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:10px}.page-row{display:block;color:var(--text);text-decoration:none}.page-row span{display:block;color:var(--cyan);font-size:13px;word-break:break-all}.goalos-legal-rail{width:min(1120px,92vw);margin:30px auto 0;border:1px solid rgba(157,255,159,.34);border-radius:18px;padding:16px;background:rgba(157,255,159,.08);color:#eaffed}.goalos-footer{width:min(1120px,92vw);margin:30px auto 0;padding:28px 0 40px;border-top:1px solid var(--line);display:flex;justify-content:space-between;gap:20px;color:#cdd7e6}.float-ask{position:fixed;right:18px;bottom:18px;z-index:30;display:flex;gap:8px}.float-ask a{border-radius:999px;text-decoration:none;color:#06100f;background:linear-gradient(90deg,var(--green),var(--cyan));padding:12px 16px;font-weight:900}.float-ask a.secondary{background:rgba(255,255,255,.12);color:var(--text);border:1px solid var(--line)}@media(max-width:900px){.command-hero,.command-grid,.detail-grid,.cards,.cards.wide,.page-list{grid-template-columns:1fr}.command-card{position:relative;top:0}h1{font-size:52px}.hero{padding:30px}.metrics{grid-template-columns:repeat(2,1fr)}.flow-row{grid-template-columns:1fr 1fr}.goalos-footer{display:block}}`;
fs.writeFileSync(path.join(assets,'goalos-v47-mission-studio.css'), css, 'utf8');

const js = `(() => {\n  const topics = ${JSON.stringify(knowledge.topics)};\n  const useCases = ${JSON.stringify(useCases)};\n  const routes = ${JSON.stringify(routeCatalog)};\n  const $ = s => document.querySelector(s);\n  const safeRoute = r => routes.includes(r) ? r : (routes.includes(r.replace(/^\\.\\//,'')) ? r.replace(/^\\.\\//,'') : 'all-pages.html');\n  function words(s){ return String(s||'').toLowerCase().replace(/[^a-z0-9]+/g,' ').split(' ').filter(Boolean); }\n  function score(q, t){ const qw = words(q); const tw = words([t.title,t.match,t.answer].join(' ')); let s = 0; for (const w of qw){ if (tw.includes(w)) s += w.length > 4 ? 4 : 2; } if (!qw.length) s = 1; return s; }\n  function best(q){ let ranked = topics.map(t => ({...t, score: score(q,t)})).sort((a,b)=>b.score-a.score); return ranked[0] || topics[0]; }\n  function useByRoute(route){ return useCases.find(u => u.recommendedRoute === route) || useCases.find(u => route.includes(u.id)) || useCases[0]; }\n  function render(){ const box = $('#goalosCommandBox'); if (!box) return; const q = box.innerText.trim(); const top = best(q); const u = useByRoute(top.route); const route = safeRoute(u.recommendedRoute || top.route); const confidence = Math.min(98, Math.max(70, 70 + top.score * 2)); const steps = ['Interpret intent','Define done','Plan evidence','Route review','Map protocol rails','Issue receipt'];\n    const title = $('#planTitle'); if (title) title.textContent = u.title || top.title;\n    const summary = $('#planSummary'); if (summary) summary.textContent = u.goalosOutcome || top.answer;\n    const conf = $('#routeConfidence'); if (conf) conf.textContent = confidence + '%';\n    const tier = $('#planTier'); if (tier) tier.textContent = String(u.tier || 'Signoff').split(' ')[0];\n    const cc = $('#criteriaCount'); if (cc) cc.textContent = (u.acceptanceCriteria||[]).length;\n    const rc = $('#railCount'); if (rc) rc.textContent = (u.protocolRails||[]).length;\n    const rb = $('#routeBox'); if (rb){ rb.href = route; rb.querySelector('span').textContent = route; }\n    const care = $('#careSteps'); if (care) care.innerHTML = steps.map((s,i)=>'<div class="step"><b>'+ (i+1) +'</b><strong>'+s+'</strong><br><span>'+['Understand the plain-language request and choose the correct mission type.','Create success criteria, boundaries, reviewer authority, and constraints.','Build the evidence checklist and proof object map.','Choose the Signoff, reviewer, protocol, or governance page.','Show which trust rails are relevant, without claiming live activation.','Prepare a synthetic acceptance record and next action.'][i]+'</span></div>').join('');\n    const cl = $('#criteriaList'); if (cl) cl.innerHTML = (u.acceptanceCriteria||[]).map(x=>'<span>'+x+'</span>').join('');\n    const el = $('#evidenceList'); if (el) el.innerHTML = (u.evidenceChecklist||[]).map(x=>'<span>'+x+'</span>').join('');\n    const rl = $('#railsList'); if (rl) rl.innerHTML = (u.protocolRails||[]).map(x=>'<span>'+x+'</span>').join('');\n    const receipt = { mission_id:'GOALOS-V47-'+Date.now().toString(36).toUpperCase(), decision_state:'SIGNOFF_READY', request:q, use_case:u.id, tier:u.tier, recommended_route:route, criteria:u.acceptanceCriteria, evidence:u.evidenceChecklist, rails:u.protocolRails, public_safe:true };\n    const rj = $('#receiptJson'); if (rj) rj.textContent = JSON.stringify(receipt,null,2);\n    window.__goalosRoute = route; window.__goalosReceipt = receipt;\n  }\n  document.addEventListener('click', e => { const ex = e.target.closest('[data-goalos-example]'); if (ex){ const box = $('#goalosCommandBox'); if (box){ box.innerText = ex.getAttribute('data-goalos-example'); render(); box.focus(); } } const role = e.target.closest('[data-goalos-role]'); if (role){ document.querySelectorAll('[data-goalos-role]').forEach(x=>x.classList.remove('active')); role.classList.add('active'); render(); } });\n  document.addEventListener('click', e => { if (e.target && e.target.id === 'runGoalosCommand') render(); if (e.target && e.target.id === 'openGoalosRoute') { render(); location.href = window.__goalosRoute || 'all-pages.html'; } if (e.target && e.target.id === 'copyGoalosPlan') { render(); const text = JSON.stringify(window.__goalosReceipt || {}, null, 2); if (navigator.clipboard) navigator.clipboard.writeText(text); } });\n  document.addEventListener('keydown', e => { if ((e.metaKey || e.ctrlKey) && e.key.toLowerCase() === 'k') { e.preventDefault(); const box = $('#goalosCommandBox'); if (box) { box.focus(); document.execCommand && document.execCommand('selectAll', false, null); } } if (e.key === 'Enter' && e.target && e.target.id === 'goalosCommandBox' && (e.metaKey || e.ctrlKey)) { e.preventDefault(); render(); } });\n  function installFloat(){ if(document.querySelector('.float-ask')) return; const d=document.createElement('div'); d.className='float-ask'; d.innerHTML='<a href="goalos-front-center.html">Tell GoalOS</a><a class="secondary" href="all-pages.html">All pages</a><a class="secondary" href="ask-goalos.html">Ask</a>'; document.body.appendChild(d); }\n  if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', () => { render(); installFloat(); }); else { render(); installFloat(); }\n})();`;
for (const name of ['goalos-v47-mission-studio.js','goalos-v39-navigator.js','goalos-v40-concierge.js','goalos-v45-command-os.js','goalos-v46-safe-concierge.js']) fs.writeFileSync(path.join(assets,name), js, 'utf8');

const worker = `export default { async fetch(request) { return new Response(JSON.stringify({ ok:true, message:'Deploy a server-side router here only after adding origin controls, rate limits, route allowlists, privacy disclosure, and secret management.' }), { headers: { 'content-type': 'application/json' } }); } };\n`;
fs.mkdirSync(path.join(root,'serverless'), {recursive:true});
fs.writeFileSync(path.join(root,'serverless','goalos-v47-command-worker.example.js'), worker, 'utf8');
write('goalos-v47-live-command-endpoint-spec.json', JSON.stringify({ version:'v47', defaultMode:'browser-local', productionEndpoint:'server-side only', requirements:['rate limits','origin allowlist','route allowlist','prompt-injection hardening','no browser API keys','privacy disclosure'] }, null, 2));

console.log(`GoalOS front-center mission studio v47 build complete: ${listHtml().length} HTML pages, ${useCases.length} use cases, ${contracts.length} contracts indexed.`);
